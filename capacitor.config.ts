import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.za.tveco.invoice',
  appName: 'TVECO Invoice Generator',
  webDir: 'dist'
};

export default config;
